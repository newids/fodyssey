# HANDOFF — 새 리포지토리 이관 전달 문서

> 작성일: 2026-08-22 | 작성 목적: 이 리포지토리(codyssey-e2-1, 미션 e2-1 텀 프로젝트)의 작업을 종료하고,
> **새 공모전 아이디어 개발**을 새 리포지토리에서 시작하기 위한 인수인계 문서.
> 새 환경의 첫 세션에서 이 파일 하나만 읽으면 무엇을 가져왔고 무엇을 버렸는지, 어떻게 시작하는지 알 수 있어야 한다.

---

## 0. 종료 선언 — 여기서 끝나는 것

| 항목 | 상태 |
|---|---|
| 미션 e2-1 (코디세이 입학연수 텀 프로젝트) | **종료.** 제출 형식(A4 2쪽·8항목·발표 10쪽), 심사 대응 포인트, 배포 게이트 검사어 전부 이 미션 전용 — 승계하지 않는다 |
| 6조 · 포디세이(Fodyssey) 팀 | **해산.** 팀 최종 기획서(6조 PDF)는 팀 공동 자산이므로 **새 리포로 가져가지 않는다** |
| 팀원 관련 사항 (실명 미정, 이니셜 5인 배정, feature-pitch-deck 브랜치) | **소멸.** 승계할 것 없음 |
| 아이디어 서사 전체 (금융 코디세이 v1~v4, 모의사기 훈련, 인프라 우산, 3모듈, 진단-연결 구조) | **은퇴 완료** (`docs/idea-1/_archive/` + 전략 v2.0 §⑤). 새 공모전에서 재활용하려면 "검증된 사실"만 가져가고 이름·서사는 새로 만든다 |
| 백로그 잔여 항목 (data.go.kr API 신청, NIA XLSX, 제출 조판, W2-4/7/9 권고) | **전부 소멸.** 미션 제출 전제의 항목들 |
| gh-pages 배포물 (기획안 웹, /pitch-deck/, /app/, 부록) | 이 리포에 남긴다. 새 리포는 배포를 새로 구성 |

## 1. 이관 대상 자산 — 새 리포로 가져갈 것

가치 순서대로. 각 항목에 원본 경로와 이관 시 손질 포인트를 병기한다.

### A. 아이디어 발전 하네스 (최우선 이관)

6라운드의 피드백 반영을 굴리며 검증된 에이전트·스킬 체계. **구조는 그대로 쓰고 내용만 도메인 교체**하면 된다.

| 자산 | 경로 | 이관 시 손질 |
|---|---|---|
| 에이전트 6종 | `.claude/agents/` — feedback-analyst, strategy-planner, proposal-writer, deck-producer, mvp-builder, judge-redteam | 역할 정의는 공모전 일반형으로 재사용 가능. 본문 속 "금융 코디세이"·미션 형식(A4 2쪽 등)·전략 파일 경로 참조를 새 공모전 요강으로 치환 |
| 스킬 6종 | `.claude/skills/` — idea-development(오케스트레이터), feedback-integration, proposal-writing, deck-production, mvp-building, judge-review | 동일. 특히 idea-development의 Phase 구조(피드백 분석 → 전략 → 산출물 병렬 → 심사 루프)는 도메인 무관하게 유효 |
| 워크스페이스 규약 | `docs/idea-1/_workspace/` 의 파일 명명 규칙 (01 피드백 결정표 → 02 전략 → 03~07 산출물 → 05 판정) | 폴더만 새로 파고 규칙 승계 |

### B. 프로세스 교훈 (하네스 운영 6라운드에서 검증된 규칙)

새 리포의 CLAUDE.md에 처음부터 박아 넣을 것. 전부 실제 반려·재작업을 겪고 만든 규칙이다.

1. **정본 문안 분책 방식** — 전략 문서는 「본문(판단·근거)」과 「정본 문안(복사 대상 문자열)」을 분리하고, 충돌 시 분책이 이긴다. 하위 에이전트가 문안을 재창작하는 사고를 구조적으로 차단.
2. **정본 문안 신설 시 적용 범위 1줄 병기** — "이 문자열은 어느 산출물의 어느 표면까지 복사 의무인가"를 명시하지 않으면 반려가 반복됨 (R2-3·R3-1 공통 원인).
3. **전략 조항 간 문자열 교차 대조 (§8-0, 5문항)** — 전략 개정 시 조항끼리 모순되면 하위 산출물 결함이 3라운드 연속 재발했다. 개정 직후 자체 대조를 절차화.
4. **지면 제약은 실측만 인정** — "대략 2쪽일 것" 추정 답 무효. 문안 확정 직후 렌더링 실측 1회 (분량 초과 반려 통산 3회의 종결 규칙).
5. **.md 수정 시 파생 .html 동일 커밋 재생성** — "정본 갱신·열람본 구버전" 사고 2회. 원본-파생물 쌍은 한 커밋에서 함께 갱신.
6. **판정 루프는 3회 상한의 레드팀 방식** — judge-redteam이 반려 사유를 ID로 관리(R#-#·W#-#), 통과 후에만 정본 반영·배포.
7. **피드백은 결정표로 변환 후 반영** — 수용/부분수용/보류 3분류 + 사유. 51건 규모도 이 방식으로 누락 없이 처리됨.

### C. 검증 데이터 자산 (새 공모전이 금융·시니어 인접 도메인일 때만)

| 자산 | 경로 | 비고 |
|---|---|---|
| 통계·출처 원천 | `PROJECT_BRIEF.md` (v1.0, 팩트체크 완료) | 수치 인용 시 기준 연도·출처 병기 원칙 포함. 서사 부분은 이미 폐기됐으니 통계만 |
| 출처 목록 22건 | `docs/idea-1/부록_출처.md` | 경찰청 국수본·금융위·KISA 등 검증 완료 출처. 주의: "50대 이상 53%"는 피해액 관점, 연령별 건수와 별개 지표 |
| 경쟁 서비스 조사 | `docs/idea-1/부록_경쟁서비스_조사.md` | 4축 비교 프레임 자체도 재사용 가치 있음 |
| 취약도 산식·용어 | `docs/idea-1/부록_용어와_아키텍처.md` | Rule-based Scoring을 계산 가능하게 만든 산식. 도메인 무관하게 "진단 지표 설계" 참고자료 |
| 공공데이터 API 조사 | `docs/idea-1/_archive/공공데이터_API_조사_v0.1.md` | data.go.kr 활용 시 재조사 비용 절감 |

도메인이 완전히 다르면 C는 가져가지 않는다 (PROJECT_BRIEF.md 하나만 참고용으로 보관 권장).

### D. MVP 코드 패턴

| 자산 | 경로 | 재사용 가치 |
|---|---|---|
| 판별 퀴즈 웹앱 | `mvp/web/` (Vite, 정적 배포) | **"완전 정적 데모" 아키텍처** — LLM 호출 없이 데이터셋 `clues`를 클라이언트에서 프로파일별 렌더링. 발표 중 네트워크·API 장애로 데모가 죽지 않는 구조. 새 공모전 MVP에도 같은 원칙 권장 |
| 데이터셋 | `mvp/data/scam_dataset_v0.1.json` | 스키마 설계(시나리오·정답·단서·난이도) 참고 |
| 적응형 프롬프트 프로토타입 | `mvp/prototype/` (run_compare.py + golden) | golden 대비 프롬프트 검증 하네스 패턴. API 키 실행 검증은 미완인 채 종료 |

`node_modules/`·`dist/`는 복사하지 말 것 (45M 중 대부분).

### E. 전역 규칙·메모리 (리포 밖 자산 — 자동 승계됨, 확인만)

- 메모리: 캡차 페이지 접근 금지(수동 다운로드 안내로 전환), 구시대적 고령자 이미지 금지 — `~/.claude/projects/.../memory/`. **주의: 메모리 디렉토리는 프로젝트 경로별로 분리되므로 새 리포에서는 새로 생성됨.** 이 두 규칙은 새 CLAUDE.md에 직접 기재할 것.
- 작업 규칙 중 승계 가치: 산출물 한국어, 웹 산출물 LaTeX/MathJax 금지(모바일 가독성), 톤(문제해결 중심·담백한 지적 호기심·낙천적 위트), 통계 인용 시 연도·출처 병기.

## 2. 이관 금지·재검토 목록

| 항목 | 처분 | 사유 |
|---|---|---|
| `docs/idea-1/_doc/` 6조 PDF | **금지** | 팀 공동 자산. 읽기 전용 SSOT였고 팀 해산으로 인용 근거 소멸 |
| `docs/idea-1/Feedback-1~6/` | **금지** | 팀원·심사위원 피드백 원본 — 타인 기여물. 교훈은 §1-B로 추출 완료 |
| `docs/idea-1/_archive/`, `_workspace/` 전략 v2.0 본문 | **금지** | 은퇴 서사. 필요한 검증 자산은 §1-C 부록으로 이미 추출돼 있음 |
| `docs/양식/` (hwpx·pdf) | **금지** | 미션 e2-1 전용 제출 양식 |
| 팀명 "포디세이(Fodyssey)"·"금융 코디세이" 명칭 일체 | **금지** | 팀·미션 귀속 브랜드 |
| 4050 AI Cafe (4050aicafe.kr) 언급 | **재검토** | 이 공모전에서 금지어였으나 사유는 공모전 맥락(검증 경로 대체). 개인 자산으로는 유효 — **새 공모전에서는 규정·심사 관점에서 재판단 후 결정** |
| `docs/idea-2/전_국민_AI_금융역량_플랫폼.md` | **재검토** | 미제출 예비 아이디어. 새 공모전 주제와 맞으면 씨앗으로 쓸 수 있음 |

## 3. 새 리포지토리 셋업 절차

```bash
# 1) 새 리포 생성 (예: idea-lab — 이름은 새 공모전 확정 후 결정)
mkdir ~/new-repo && cd ~/new-repo && git init

# 2) 하네스 복사 (A)
cp -R <이_리포>/.claude .claude

# 3) 참고 자산 복사 (C·D — 도메인 판단 후 선택)
mkdir -p reference/mvp-patterns
cp <이_리포>/PROJECT_BRIEF.md reference/
cp <이_리포>/docs/idea-1/부록_출처.md reference/          # 금융 인접 도메인일 때만
cp <이_리포>/docs/idea-1/부록_경쟁서비스_조사.md reference/
cp <이_리포>/docs/idea-1/부록_용어와_아키텍처.md reference/
rsync -a --exclude node_modules --exclude dist <이_리포>/mvp/ reference/mvp-patterns/

# 4) .gitignore
printf '.DS_Store\nnode_modules/\ndist/\n' > .gitignore

# 5) 첫 세션에서: 이 HANDOFF.md를 리포에 넣고
#    "새 공모전 요강을 기준으로 CLAUDE.md와 하네스를 재구성해줘" 로 시작
#    (하네스 재구성은 harness 스킬, 에이전트·스킬 내부의 미션 특화 문자열 치환 포함)
```

새 CLAUDE.md 필수 골격 (이 리포에서 검증된 형태):

1. 프로젝트 한 줄 요약 + 새 공모전 요강 문서 경로(SSOT 지정)
2. 기준 문서 층위 (L0 요강 / L2 전략 / L3 산출물)
3. 작업 규칙 (§1-E의 승계 규칙 + 새 공모전 고유 제약)
4. 하네스 트리거 정의 + **변경 이력 표** (라운드별 교훈 축적 — 이 리포에서 가장 효과 컸던 장치)
5. 백로그 (완료/잔여 구분)

## 4. 첫 작업 제안 (새 리포 1일차)

1. 새 공모전 요강 입수 → `docs/` 에 배치, CLAUDE.md에 SSOT로 등재
2. 하네스 문자열 치환 라운드 — 에이전트·스킬 6+6종에서 "금융 코디세이"·"A4 2쪽·8항목"·"docs/idea-1" 경로 참조를 새 요강 기준으로 교체 (judge-redteam의 검증 루브릭이 특히 미션 형식에 강결합돼 있음)
3. 아이디어 발산은 이 리포의 2026-08-15 방식 재사용: 트렌드 조사 → 후보 N종 도출 → 공모전 정합성 기준 재우선순위화 → 팩트체크 → 확정본 v1.0

---

*이 문서는 codyssey-e2-1 `newids/feature-jschoi` 브랜치 최종 커밋 기준으로 작성됨. 원본 리포는 아카이브 상태로 보존.*
